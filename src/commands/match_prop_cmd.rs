//! Match properties command (stub).

/// MATCHPROP command — applies visual properties (color, linetype,
/// lineweight, layer) from a source entity to destination entities.
///
/// Full implementation in Step 6.
pub struct MatchPropCommand;
