//! PropertyPalette (stub)

/// A curated set of named colors, linetypes, and lineweights available
/// to the user via the property palette UI.
///
/// Full implementation in Step 3.
#[derive(Debug, Clone, Default)]
pub struct PropertyPalette;
