//! PropertyResolver (stub)

/// Resolves visual properties (color, linetype, lineweight) through
/// the ByLayer / ByBlock / Explicit inheritance chain.
///
/// Full implementation in Step 3.
#[derive(Debug, Clone, Default)]
pub struct PropertyResolver;
