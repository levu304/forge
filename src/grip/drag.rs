//! GripDragState (stub)

/// Tracks the state of an active grip-drag operation (grip edit / STRETCH).
///
/// Full implementation in Step 5.
#[derive(Debug, Clone, Default)]
pub struct GripDragState;
