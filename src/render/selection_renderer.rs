//! Selection highlight renderer (stub).
//!
//! Renders selection highlights: thicker entity lines, blue tint
//! (via per-instance `is_selected` attribute), and bounding-box
//! overlays around the selected set.
