//! Render pipeline for block-instance geometry.

/// Renders block reference (BlockRef) entities by traversing the block
/// definition's entity list and issuing per-entity draw calls.
///
/// Full implementation in Step 6.
#[allow(dead_code)]
pub struct BlockRenderer;
